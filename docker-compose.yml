services:
  nycdb:
    build:
      context: .
      args:
        PYTHON_VERSION: "${PYTHON_VERSION:-3.12-bookworm}"
    volumes:
      - ./src:/nycdb/src
      - download-data:/nycdb/src/data

volumes:
  download-data: